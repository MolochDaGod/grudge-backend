{
  "name": "grudge-badge-reader",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that JWT-validates requests at the edge before forwarding to the Grudge backend.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
